# !! 이 파일은 더 이상 사용되지 않습니다 !!
# nginx → Apache2 전환 완료 (2026-06-01)
# VPS 웹서버: Apache2 (host-native, nginx purged)
# Apache2 설정 위치:
#   /etc/apache2/sites-enabled/000-xn--2e0bj1fruw33b6ti.net.conf       (HTTP :80)
#   /etc/apache2/sites-enabled/000-xn--2e0bj1fruw33b6ti.net-le-ssl.conf (HTTPS :443)
#   /var/www/xn--2e0bj1fruw33b6ti.net/public/.htaccess
#
# 이 디렉토리(nginx/)는 하위 호환 참조용으로 유지되나 실제로는 읽히지 않습니다.
# Apache 동등 설정은 apache/ 디렉토리를 참조하세요.
